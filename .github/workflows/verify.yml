name: Verify by Library Checker

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  RUST_VERSION: "1.85.0"

jobs:
  verify:
    name: ${{ matrix.category }} ${{ matrix.problem }}
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      fail-fast: false
      matrix:
        include:
          - category: data_structure
            problem: unionfind
          - category: data_structure
            problem: unionfind_with_potential
          - category: data_structure
            problem: unionfind_with_potential_non_commutative_group
          - category: data_structure
            problem: point_set_range_composite
          - category: data_structure
            problem: range_affine_range_sum

          - category: convolution
            problem: convolution_mod

          - category: number_theory
            problem: primality_test
          - category: number_theory
            problem: factorize
          - category: number_theory
            problem: primitive_root

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          source $HOME/.cargo/env

      - name: Cache cargo dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install cargo-make
        run: cargo install cargo-make

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3

      - name: Clone library-checker-problems
        working-directory: verify
        run: |
          git clone https://github.com/yosupo06/library-checker-problems.git

      - name: Verify ${{ matrix.problem }}
        working-directory: verify
        timeout-minutes: 5
        run: |
          CATEGORY=${{ matrix.category }} PROBLEM=${{ matrix.problem }} cargo make verify
